# Tests

Run from the repository root:

```sh
python .muse/candidates/saitousan-live-poc-review/tests/test_skill_contract.py
```

These tests use only Python's standard library and validate that the Skill candidate includes the required project-specific guardrails, source-of-truth references, phase boundaries, and safety constraints.
